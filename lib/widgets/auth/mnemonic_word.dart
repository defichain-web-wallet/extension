import 'package:flutter/material.dart';

class MnemonicWorld extends StatelessWidget {
  const MnemonicWorld({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(

      ),
      child: RichText(
        text: TextSpan(
          children: [
            TextSpan(),
            TextSpan(),
          ]
        ),
      ),
    );
  }
}
